const fmt = import("fmt")

struct User {
    name,
    age,
    job,
}

fn User.info() {
    fmt.Println("Hello world")
    let salary = 1000 + 1
    fmt.Println(salary)
}

let u = User{name:"Sobri", age:99, job:"Programmer"}
User.info() // notice it is User and not u